import { TESTING } from 'constants/index.ts';
import { ChatColors } from 'types/index.ts';

export const chatColors: ChatColors = {
  'chat.requestBackground': TESTING,
  'chat.requestBorder': TESTING,
};
