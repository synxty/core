import { colors } from '@synxty/design-tokens';
import { WindowBorder } from 'types/index.ts';

const { black } = colors;

export const windowBorder: WindowBorder = {
 "window.activeBorder": black.tint[20],
 "window.inactiveBorder": black.tint[20]
};
