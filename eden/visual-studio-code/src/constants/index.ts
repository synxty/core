import { SupportedThemes } from '@synxty/brand-assets/apps-specs';
import { aliases } from '@synxty/design-tokens';

export const ICON_THEME: SupportedThemes = 'dark';

export const THEMES_DIRECTORY = 'themes';
export const DARK_THEME_PATH = `${THEMES_DIRECTORY}/dark.json`;
export const LIGHT_THEME_PATH = `${THEMES_DIRECTORY}/light.json`;

export const TESTING = aliases.purple.DEFAULT;
export const DEBUGGING = aliases.danger.DEFAULT;
